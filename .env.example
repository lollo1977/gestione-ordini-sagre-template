# Database
DATABASE_URL=postgresql://username:password@hostname:port/database

# Environment
NODE_ENV=development
PORT=5000

# Render.com fornirà automaticamente questi valori:
# DATABASE_URL (dal PostgreSQL service)
# PORT (automatico)